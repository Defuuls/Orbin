# Orbin 148 — Nectarine
#
# Merging this file to main triggers the Cut Release workflow, which opens the
# release PR. See release/README.md.

number       = 148
codename     = "Nectarine"
version_code = 166

# One line for the README's "What's new in <number>:" highlight. Optional —
# leaving it out removes that line rather than letting it name a stale release.
highlight = "The feed is sorted by board, A–Z, under a heading for each board."

summary = [
  "bump Orbin to 148-Nectarine / versionCode 166",
  "sort the feed by board A–Z, under a heading per board",
]

[changelog]
Changed = [
  "The feed is sorted by board, A–Z, under a heading for each board, with the most recently active threads first within each board. It was one list ordered by activity across every board (#560).",
]

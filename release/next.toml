# Orbin 140 — Elderberry
#
# Merging this file to main triggers the Cut Release workflow, which opens the
# release PR. See release/README.md.

number       = 140
codename     = "Elderberry"
version_code = 158

# One line for the README's "What's new in <number>:" highlight. Optional —
# leaving it out removes that line rather than letting it name a stale release.
highlight = "A bottom chrome of just Feed and Settings, with Search, Downloads and Commands moved into Settings › Library."

summary = [
  "bump Orbin to 140-Elderberry / versionCode 158",
  "remove the Go button and re-home its destinations in Settings › Library",
]

[changelog]
Changed = [
  "Removed the Go button from the bottom chrome. Search, Downloads and the command sheet now live in Settings › Library, alongside Boards and All media, leaving Feed and Settings the only things competing for the strip (#515).",
  "Removed settings rows that could no longer be reached, so the list and its search index describe only what the app actually offers (#514).",
]
Fixed = [
  "Corrected the README, which advertised what was new in an old release beside a current one. The release manifest now owns that line, so it moves with each release or disappears rather than going stale (#512, #513).",
]

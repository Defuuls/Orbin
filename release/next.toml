# Orbin 152 — Raspberry
#
# Merging this file to main triggers the Cut Release workflow, which opens the
# release PR. See release/README.md.

number       = 152
codename     = "Raspberry"
version_code = 170

# One line for the README's "What's new in <number>:" highlight. Optional —
# leaving it out removes that line rather than letting it name a stale release.
highlight = "Foldables and tablets can show the feed in more columns."

summary = [
  "bump Orbin to 152-Raspberry / versionCode 170",
  "pick feed columns on foldables and tablets",
]

[changelog]
Added = [
  "Foldables and tablets are detected automatically. A foldable's front screen shows the feed in one column; unfolded, Settings → **Feed columns when unfolded** picks 1–3 columns. Tablets get **Feed columns**, 1–4. Both start at 2, and the setting only appears on those devices (#583).",
]

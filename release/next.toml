# Orbin 154 — Tangerine
#
# Merging this file to main triggers the Cut Release workflow, which opens the
# release PR. See release/README.md.

number       = 154
codename     = "Tangerine"
version_code = 172

# One line for the README's "What's new in <number>:" highlight. Optional —
# leaving it out removes that line rather than letting it name a stale release.
highlight = "Downloads takes the middle tab, where Media used to be."

summary = [
  "bump Orbin to 154-Tangerine / versionCode 172",
  "make the middle tab Downloads instead of Media",
]

[changelog]
Changed = [
  "The middle tab is now **Downloads**, listing everything you saved with its progress and a retry for failed files. It replaces the Media tab and its All media wall; each board's own media wall stays, reached from its catalog, and its **Saved ›** opens Downloads.",
]

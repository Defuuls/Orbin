# Orbin 150 — Papaya
#
# Merging this file to main triggers the Cut Release workflow, which opens the
# release PR. See release/README.md.

number       = 150
codename     = "Papaya"
version_code = 168

# One line for the README's "What's new in <number>:" highlight. Optional —
# leaving it out removes that line rather than letting it name a stale release.
highlight = "Nothing plays in the feed; GIFs and videos wait until you open the thread."

summary = [
  "bump Orbin to 150-Papaya / versionCode 168",
  "stop autoplaying media in the feed",
]

[changelog]
Changed = [
  "Nothing plays in the feed any more. GIFs and videos show their thumbnail until you open the thread; the feed used to play the card it was centred on (#579).",
]

<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />

    <application
        android:name=".OrbinApplication"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="false"
        android:enableOnBackInvokedCallback="true"
        android:theme="@style/Theme.Orbin.Starting">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Orbin.Starting" />

        <!--
          One alias, permanently enabled, carrying the launcher entry. There were five, one per
          selectable app icon, toggled at runtime; the choice is gone but this one stays, because
          it is the component a launcher pins. Deleting it and putting the intent-filter on
          MainActivity would break every pinned shortcut and every home-screen icon belonging to
          someone on the default icon — which, the setting being off by default, is nearly
          everyone. Keeping the name keeps those working, and a component name is an identifier
          rather than a label, so the fact that it still says "OrbitalOrb" is a matter of history
          and not of what it draws.

          Compatibility is the whole of the reason. An earlier version of this comment also
          claimed the alias was needed to keep `OrbinAppTest` alive, which was wrong:
          ActivityScenario launches the activity under test with MAIN/LAUNCHER itself, whatever
          the manifest says, so moving the filter changes nothing there.

          No `android:icon` here on purpose — the alias inherits the application's, so the icon is
          declared in exactly one place.
        -->
        <activity-alias
            android:name=".OrbitalOrbIconAlias"
            android:enabled="true"
            android:exported="true"
            android:label="@string/app_name"
            android:targetActivity=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity-alias>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- We provide our own Hilt-aware WorkManager configuration via Configuration.Provider,
             so the default on-startup initializer must be removed. -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>
    </application>

</manifest>

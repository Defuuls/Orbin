<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Only what a reader needs. No notifications, no biometrics: the features behind those
         permissions are the ones this build leaves out. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- tools:ignore="NotificationPermission": the shared :data module carries the thread-watch
         notifier, so lint sees a notification call site and asks for POST_NOTIFICATIONS. This app
         has no thread watching to schedule it and never calls WatchScheduler, so that code is
         unreachable here. Declaring the permission to satisfy lint would be the worse answer —
         it would ask readers to grant notifications to an app that has none. -->
    <application
        android:name=".MinimalApplication"
        tools:ignore="NotificationPermission"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/minimal_app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="false"
        android:enableOnBackInvokedCallback="true"
        android:theme="@style/Theme.OrbinMinimal">

        <activity
            android:name=".MinimalActivity"
            android:exported="true"
            android:label="@string/minimal_app_name"
            android:theme="@style/Theme.OrbinMinimal">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- WorkManager's default initializer is removed for the same reason as in the full
             client: the Hilt-aware configuration below replaces it. -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>
    </application>

</manifest>
